{
  "name": "shopeasy-ecommerce-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  },
  "engines": {
    "node": "22.x"
  },
  "packageManager": "pnpm@11.10.0",
  "overrides": {
    "@mastra/core": "1.50.1"
  }
}
